spring:
  application:
    name: demoapp

  datasource:
    url: jdbc:postgresql://localhost:5432/financial_toolkit_test
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yml

server:
  port: 8080
