spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/financial_toolkit
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
